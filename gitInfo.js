/*
    For this section of the assessment you will be putting together a cheat sheet for common git commands.  
    You'll provide the command as well as what it does. 
*/

//////////////////PROBLEM 1////////////////////
/*
    Create a variable called 'gitDefinition'.
    It should be a string containing your best definition of what Git is.
*/

let gitDefinition = ("Git is a version control system software used by people in the tech field, typically used for tracking changes in files, taking snapshots, and working with other programmers. ")

//////////////////PROBLEM 2////////////////////
/*
    Create a variable called 'gitHubDefinition'.  
    It should be a string containing your best definition of what GitHub is.
*/

let gitHubDefinition = ("Github is a open source code hosting platform for collaboration between programmers. It can receive code and keep it stored on the site, as well as commits/changes to the file you have uploaded, so lang as you push it through.")

//////////////////PROBLEM 3////////////////////
/*
    Create a variable called 'gitInitDefinition'.  
    It should be a string containing your best definition of what 'git init' does.
*/

let gitInitDefinition = ("Git init is a command that initializes your repository, making you able to upload the code you have written to github.")

//////////////////PROBLEM 4////////////////////
/*
    Create a variable called 'gitCloneDefinition'.  
    It should be a string containing your best definition of what 'git clone' does.
*/

let gitCloneDefinition = ("Git clone is a command that can be used in your VSC or alternative to copy a source code from github, and allow it to be uploaded onto your computer via git.")

//////////////////PROBLEM 5////////////////////
/*
    Create a variable called 'gitStatusDefinition'.  
    It should be a string containing your best definition of what 'git status' does.
*/

let gitStatusDefinition = ("Git status is a command that can be used to check the current status of your git file, noting any unsaved changes, or changes in general to your file.")

//////////////////PROBLEM 6////////////////////
/*
    Create a variable called 'gitAddDefinition'.  
    It should be a string containing your best definition of what 'git add' does.

    Create another variable called 'gitAddCode'.  
    It should be a string containing the code to add all files.
*/

let gitAddDefinition = ("Git add makes a change in the current directory, telling your computer to include any changes made in the next commit.")
let gitAddCode = ("git add .")
//////////////////PROBLEM 7////////////////////
/*
    Create a variable called 'gitCommitDefinition'.  
    It should be a string containing your best definition of what 'git commit' does.

    Create a variable called 'gitCommitCode'.  
    It should be a string containing the code to commit using the message "initial commit".
*/

let gitCommitDefinition = ("Git commit takes a 'snapshot'of your current projects changes, this can be a way to save work.")
let gitCommitCode = ("git commit -m 'initial commit'")


//////////////////PROBLEM 8////////////////////
/*
    Create a variable called 'gitPushDefinition'.  
    It should be a string containing your best definition of what 'git push' does.
*/

let gitPushDefinition = ("Git push is a command that will take all your current commited work, and push it through to git, so it can be viewed on the site. The site tracks commits, and when you push so you can see what you have done to your code each time you push it through.")